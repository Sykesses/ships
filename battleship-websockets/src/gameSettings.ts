import { ShipAvailable } from './types';

export const gameSettings: {
  gameId: number,
  fieldSize: number,
  shipsAvailable: ShipAvailable[]
} = {
  'gameId': 0,
  'fieldSize': 0,
  'shipsAvailable': []
};